# Screenshots

Add these two screenshots and commit them:

## swagger.png
1. `docker compose up --build`
2. Open http://localhost:8000/docs
3. Screenshot the full page showing all endpoints expanded

## rabbitmq.png
1. Open http://localhost:15672 (guest / guest)
2. Go to the **Queues** tab → click `autorent.events.queue`
3. Screenshot showing message rates / queue depth
